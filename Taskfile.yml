# https://taskfile.dev
version: "3"

tasks:
  default:
    cmds:
      - task --list
    silent: true

  new-mdxslide:
    desc: Create a personal MDX presentation
    cmds:
      - node scripts/new-mdxslide.mjs personal {{.CLI_ARGS}}

  new-mdxslide-corp:
    desc: Create a corporate MDX presentation
    cmds:
      - node scripts/new-mdxslide.mjs corporate {{.CLI_ARGS}}

  enable-markdown-for-agents:
    desc: Enable Cloudflare Markdown for Agents on the zone
    summary: |
      Usage:
        task enable-markdown-for-agents

      Required env vars:
        CLOUDFLARE_API_TOKEN - API token with Zone Settings edit permission
        CLOUDFLARE_ZONE_ID   - Target zone ID

      Or configure via Cloudflare dashboard:
        AI > AI Crawl Control > Markdown for Agents → On
    cmds:
      - ./scripts/enable-markdown-for-agents.sh

  pdf:
    desc: Export a presentation to PDF using decktape
    summary: |
      Usage:
        task pdf -- <slide-name>
        task pdf -- <slide-name> output.pdf

      Examples:
        task pdf -- tacotuesday
        task pdf -- better-t-stack my-slides.pdf

      Requirements:
        - decktape must be installed: npm install -g decktape
    cmds:
      - ./scripts/pdf-export.sh {{.CLI_ARGS}}
